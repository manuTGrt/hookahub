import 'package:flutter/material.dart';

// Paleta pastel original
const pastelBlue = Color(0xFFA7C7E7);
const pastelPink = Color(0xFFF7C5CC);
const pastelGreen = Color(0xFFB7E5B4);
const pastelYellow = Color(0xFFF0E68C); // Amarillo suave más equilibrado
const pastelPurple = Color(0xFFD7B4F3);

// Paleta turquesa y complementarios (modo claro)
const turquoise = Color(0xFF72C8C1); // principal
const turquoiseDark = Color(0xFF3A9B94); // acento oscuro
const fieldLight = Color(0xFFE0F7F4); // turquesa muy claro para campos
const navy = Color(0xFF23404A); // azul profundo

// Paleta modo oscuro
const darkBg = Color(0xFF1A2326); // fondo principal
const darkTurquoise = Color(0xFF4DB6AC); // turquesa oscuro
const darkNavy = Color(0xFFB2DFDB); // texto claro
const fieldDark = Color(0xFF26343A); // gris azulado para campos
const turquoiseSecondaryDark = Color(0xFF274242); // acento secundario oscuro

// Alertas / avisos
const warningRed = Color(0xFFD84343);
const warningSurfaceLight = Color(0xFFFFECEB);
const warningSurfaceDark = Color(0xFF3B2020);
